# random-points-player

A simple application for displaying an array of random points generated in a background thread.
To build the application, you will need to install `Qt6` and its `Qt::Charts` module. 
